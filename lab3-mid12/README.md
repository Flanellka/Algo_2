# lab3 Амортизационный анализ

Иногда надо не писать код, а думать

Это задача максимально неудобная для всех, но она нужна. Ваша задача будет заключаться в проведении амортизационного анализа для взятого вами варианта. Тут не надо писать код, тут надо описать и доказать время работы

### Внимание! Число мест на одну вариацию лабораторной ограниченно. Приветствуем реинкарнацию так называемой *Выборности.* 

#### пояснение в две строчки: 30 чел захотели делать hard14, а всего хардов 4 штуки - тогда по 7.5 человек на одну вариацию. кто первый записался того и тапки.

| Вариация | Задача | Описание | Ограничения |
|-----------|--------|----------|-------------|
| light1 | Изменение размера динамического массива | Все помнят как работает вектор std::vector в c++ Изначально он выделяет память под N элементов, после как вектор заполняется - он увеличивает бронь памяти до 2 * N. Когда кол-во элементов сокращается до N // 4, то объем памяти нужно уменьшить до N // 2. Вам надо доказать, что амортизационное время вставки и удаления элементов при таком подходе составляет O(1) | Метод бухгалтерского учета |
| light2 |  |  | Метод потенциалов |
| light3 | Двоичный счетчик | Есть двоичный счетчик, например 0000. Первый инкремент инкрементирует первый малый бит - 0001. Второй и последующие - обнуляют единицы, и инкрементируют первый встреченный 0 - 0010, 0011, 0100, 0101 и тд. Необходимо доказать что при постоянной инкрементации, амортизационное время работы будет составлять O(1) | Метод бухгалтерского учета |
| light4 |  |  | Метод потенциалов |
| light5 | Операции с хеш-таблицами | Все считают что вставка, получение и удаление элемента в хеш-таблице составляет O(1). Однако почему? Ведь невозможна ситуация, когда нет коллизий. Вам необходимо доказать, что амортизационное время этих операций действительно составляет O(1) | Цепочечный метод разрешения коллизий, Метод бухгалтерского учета |
| light6 |  |  | Цепочечный метод разрешения коллизий, Метод потенциалов |
| middle7 |  |  | Свободная адресация для разрешения коллизий, Метод бухгалтерского учета |
| middle8 |  |  | Свободная адресация для разрешения коллизий, Метод потенциалов |
| middle9 | Операции вставки и удаления в бинарных деревьях | Утверждается, что амортизационное время O(k), или O(log(N))? Нужно выполнить амортизационный анализ и посчитать, какое будет время вставки и удаления элементов | Метод бухгалтерского учета |
| middle10 |  |  | Метод потенциалов |
| middle11 | Операции вставки и удаления в красно-черных деревьях | Утверждается, что амортизационное время O(log(N)). Нужно доказать, что амортизационная сложность будет действительно O(log(N)) | Метод бухгалтерского учета |
| middle12 |  |  | Метод потенциалов |
| hard15 | Градиентный спуск | Амортизация при стохастическом градиентном спуске: Есть: - N - Размер датасета - b - Размер батча - k - Кол-во шагов, после которых производится шаг градиента для всего датасета, k < (N / b). При обучении нейросети используются мини-батчи, но каждые k шагов в рамках эпохи производится прогон на всем датасете. Утверждается, что амортизационная стоимость алгоритма может быть O(b). Вам необходимо подобрать наборы N, b и k, при которых амортизационная стоимость равна O(b) | Можно использовать метод бухгалтерского учета или метод потненциалов, но есть вероятность что в чистую эти методы ничего не докажут, так что думайте и проводите аналогии |
| hard16 | Splay-деревья | Саморегулирующиеся бинарные деревья поиска, где операции перемещают часто используемые узлы ближе к корню (splay-операция). Нужно доказать, что изменение потенциала компенсирует стоимость вращений. Требуются нетривиальные математические выкладки (лемма о перемещении вверх и лемма о такси) | Метод потенциалов |
| hard17 | Динамические хэш-таблицы с инкрементальным рехэшированием | Вместо мгновенного удвоения таблицы при переполнении, элементы постепенно перемещаются в новую таблицу. Доказать, что сложность вставки останется O(1) при большом количестве коллизий | Метод бухгалтерского учёта |

### [Записаться](https://docs.google.com/spreadsheets/d/1HHxofhSXYhFsMowxl03EuU_gEBdFKOXmeYFhlOenG0E/edit?usp=sharing)


## Требования к отчету


0. TeX формат. В том числе титульник.

1. Все доказательства аналогично рассуждениям на лекции. `кто не был - их проблемы (c)`

Для метода бух учета:
+ Указать вводные данные - сколько монет изначально (если они есть), стоимость каждого действия
+ Разбор пошаговый тест кейсов (как минимум одного). Для данного методы суммарное количество монет не должно уходить в космов и не должно в минус уходить
+ Вывод, почему можно считать доказательство достаточным

Для метода потенциала:
+ Необходимо ввести функцию подсчета потенциала в текущем состоянии структуры данных
+ После каждого действия, производимого со структурой, нужно считать потенциал
+  Будет набор чисел, из которого можно сделать набор разности меж ближайшими соседями

2. Защита только очно.

### [Отправить](https://docs.google.com/forms/d/e/1FAIpQLSfZ0WQ4x6rmW7IqYcdXyTGOzBG8BCIqfK3PyRC4_8Ry-N9MYA/viewform?usp=dialog)

если прикладываете ссылку - убедитесь, что мы можем ее просмотреть и нас не встретит окно авторизации. 